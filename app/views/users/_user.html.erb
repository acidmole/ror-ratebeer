<div id="<%= dom_id user %>">
  <p>
    <strong>Username:</strong>
    <%= user.username %>
  </p>

  <p>
    <% if user.active %>
      <span class="badge badge-warning">account closed</span>
    <% end %>
  </p>

</div>
